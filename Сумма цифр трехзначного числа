package com.javarush.task.task03.task0301;

public class Solution {
    public static void main(String[] args) {
        int number = 594;
//        System.out.println(number);

//        int a = number%10;
//        System.out.println(a);
//        int b = number%100;
//        System.out.println(b);
//        int d = b%10;
//        System.out.println(b);
//        int c = number%1000;
//        System.out.println(c);
        /*Псевдокод, логика следующаа, символ числа мы получаем, если делим число на остаток от 10
        делать это надо в цикле, чтобы мы могли задать не только трехзначное, а любое число
        Нужно будет ввести новую переменную, в которую будет каждую итерацию цикла добавляться
        остаток от деления, либо менять саму переменную, которая изначально задана. Но проще делить на 10 в степени,
        чтобы получать символы, а степень будет зависеть от того, сколько цифр в числе, надо попробовать через длину
        массива, если необходимо, то перевести в стринг, итак пошагово действия такие:
        - Вводим переменную count, которая будет равна количеству цифр в числе

        Второй вариант - это поделить число на 100, а потом округлить до целого, потом на 10 поделить и округлить и т.д.
        Пробуем - работает и без округления:
         */
        int a = number/100;

        System.out.println(a);

        /*Дальше, чтобы осталось 2 последние цифры, мы вычитаем из изначального числа первую цифру, умноженную на 100,
        но в дальнейшем нам по-любому понадобится вынимать количество цифр в числе
         */

        int b = (number - a*100)/10;
        System.out.println(b);

        int c = (number - a*100-b*10)/1;
        System.out.println(c);

        int result = a+b+c;
        System.out.println(result);
    }
}
